[build]
buildCommand = "npm install -g pnpm@9 && pnpm install --frozen-lockfile && pnpm --filter @hugg/api build"
installCommand = "echo 'skip'"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
