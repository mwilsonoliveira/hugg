[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install -g pnpm && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @hugg/api build"]

[start]
cmd = "node apps/api/dist/index.js"
